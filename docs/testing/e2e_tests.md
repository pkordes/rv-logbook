python scripts/spec-format.py --lang e2e frontend/e2e

Trip lifecycle:
 ✔ create trip → add stop with tag → view timeline → export CSV → delete

Tags management:
 ✔ create tag → rename tag → delete tag