# Normalize line endings to LF in the repository for all text files.
# On checkout, Windows gets CRLF; macOS/Linux get LF.
* text=auto eol=lf

# Force these to always be LF even on Windows checkouts.
*.sh text eol=lf
Makefile text eol=lf

# Binaries — never alter line endings.
*.png binary
*.jpg binary
*.jpeg binary
*.svg binary
*.ico binary
*.woff binary
*.woff2 binary
