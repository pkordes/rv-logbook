# RV Logbook — environment variables
# Copy this file to .env and fill in real values.
# .env is gitignored — never commit it.
#
# Usage:
#   cp .env.example .env
#
# The Makefile loads .env automatically via `-include .env`.

# ---------------------------------------------------------------------------
# Go API server
# ---------------------------------------------------------------------------

# Port the API server listens on.
PORT=8080

# Structured log level: debug | info | warn | error
LOG_LEVEL=info

# ---------------------------------------------------------------------------
# Database
# ---------------------------------------------------------------------------

# Connection string for the dev database (used by `make backend/run` and `make db/migrate`).
# Matches the default credentials in docker-compose.yml.
DATABASE_URL=postgres://rvlogbook:rvlogbook@localhost:5432/rvlogbook?sslmode=disable

# Connection string for the test database (used by `make backend/test` integration tests).
TEST_DATABASE_URL=postgres://rvlogbook:rvlogbook@localhost:5432/rvlogbook_test?sslmode=disable

# ---------------------------------------------------------------------------
# Postgres container (used by docker-compose.yml / podman-compose)
# Only needed if you want to override the compose defaults.
# ---------------------------------------------------------------------------

# POSTGRES_USER=rvlogbook
# POSTGRES_PASSWORD=rvlogbook
# POSTGRES_DB=rvlogbook
# POSTGRES_PORT=5432
